# Curriculum track membership — the single source of truth for which lessons
# belong to the "mini" vs "full" course.
#
# The lesson directories under this folder stay FLAT (L01, L02, ... L25); a
# lesson belongs to a track by membership here, not by its location on disk.
# This mirrors docs/origin/CURRICULUM_PRD.md — `full` is the master lesson
# plan, and `mini` is the "Condensed Mini Lesson Plan" cut. `mini` must always
# be a subset of `full` (every mini lesson is also a full lesson), and both
# lists are in teaching order.
#
# EXCEPTION (for now): L50, the mini-project walkthrough capstone, is
# registered on `mini` ONLY per request — it is intentionally NOT in `full` yet,
# so it is the one lesson that breaks mini ⊆ full. See the `<need input>` note in
# docs/origin/CURRICULUM_PRD.md (just below the master lesson-plan table): decide
# whether L50 should also join `full`, then add "L50" to `[full].order` here.
#
# When you add, remove, or renumber a lesson: update the PRD table AND this
# file together so they don't drift.
#
# The `K<NN>` PREWORK track is separate from the course proper: it is required,
# gated setup students complete BEFORE L01 (environment, keys/config, Jupyter,
# reading typed/pydantic/async code, and a mandatory Docker stack). `K` sorts
# ahead of `L` so it needs no `L<NN>` renumbering. Prework gates BOTH the mini
# and full tracks — it is not a subset relationship, it is a prerequisite.

[prework]
# Required, gated prework — completed before L01 by every student, both tracks.
# Docker (K06) + keys (K02) are hard environment gates; K04/K05 (types/pydantic
# and async) are understanding gates the agent arc assumes.
order = ["K01", "K02", "K03", "K04", "K05", "K06"]
# <need input: does the mini track require the full K01–K06, or a reduced prework
# set? Current opinionated stance: mini requires all six — Docker+keys are
# universal, and K04/K05 gate the agent arc the mini also reaches (L10–L13).>
mini = ["K01", "K02", "K03", "K04", "K05", "K06"]
full = ["K01", "K02", "K03", "K04", "K05", "K06"]

[full]
# Master lesson plan — all 25 lessons, taught in numeric order.
order = [
  "L01", "L02", "L03", "L05", "L06", "L07", "L08", "L09", "L10",
  "L11", "L12", "L13", "L14", "L15", "L16", "L17", "L18", "L19", "L20",
  "L21", "L22", "L23", "L24", "L25",
]
# L04 is a reserved gap (former sequential-chaining lesson merged into L03 on 2026-07-09).

[mini]
# Condensed ~32 hr cut: the five-takeaway core (tool design, the directed pipeline
# of L03 + L05, the free-form agent, tracing & eval, and the hackathon project) plus
# chain-of-thought prompting (L06) and Skills (L22) and skill patterns &
# composition (L23) by explicit request.
# Closes with L50, the mini-project walkthrough — a project-capstone build
# (tool -> agent loop -> trace -> eval), parked at L50 so future lessons can slot
# before it without renumbering. Mini-only for now (see EXCEPTION note up top).
order = [
  "L01", "L02", "L03", "L05", "L06", "L07", "L08", "L10", "L11",
  "L12", "L13", "L22", "L23", "L50",
]

[titles]
K01 = "Environment & tooling"
K02 = "Keys & the config seam"
K03 = "Jupyter workflow"
K04 = "Python you'll read: types & pydantic"
K05 = "Async concepts"
K06 = "Docker & the multi-container stack"
L01 = "LLM and token basics"
L02 = "Prompting fundamentals"
L03 = "Directed graphs: from one node to a sequential chain"
# L04 — reserved gap (merged into L03, 2026-07-09)
L05 = "Conditional graphs: routing & branching"
L06 = "Teaching an LLM to think via prompting"
L07 = "Tool calling: how it works"
L08 = "Designing good tools"
L09 = "MCP: packaging tools as a portable contract"
L10 = "Cyclic graphs: the ReAct agent loop"
L11 = "Shallow agents in LangGraph"
L12 = "What an agent generates: state, logs, traces & extracts"
L13 = "Evaluation: first pass"
L14 = "Choosing model power for the task"
L15 = "LangGraph design patterns"
L16 = "Agent middleware and conditional tools"
L17 = "Human-in-the-loop and approval gates"
L18 = "Deep agents vs. shallow agents"
L19 = "Static system prompt vs. context management"
L20 = "Embeddings and vector similarity"
L21 = "RAG pipeline"
L22 = "Skills: just-in-time capabilities for agents"
L23 = "Skill patterns & composition"
L24 = "Multi-agent / subagent architecture (stretch)"
L25 = "Evaluation revisited"
L50 = "Tying it all together: mini project"
