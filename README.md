# fluffy-potato-curriculum

Python curriculum: code + materials. Driven via Claude Code.

## Quick start

```sh
uv sync
uv run pytest
git config core.hooksPath .githooks   # one-time, enables the commit-message hook
```

See [CLAUDE.md](./CLAUDE.md) for the full toolchain and conventions.
