# Diagram-coverage review — L08–L11 vs the generous-visuals guidance

**Status: DONE (2026-07-06).** Every item below is checked: outline densification, the deck
colour-bug fixes, the full L08–L11 deck rebuild, and the L0905 cover-badge question. Unlike the
[L01–L07 sibling](2026-07-06-1408-diagram-review-l01-l07.md), this pass queued no notebook items,
so nothing remains. Safe to delete.

Point-in-time review (2026-07-06) of the L08–L11 lecture materials against the diagram
guidance in [LECTURES.md](../origin/LECTURES.md) and
[FRONTEND-STYLE.md](../../src/fluffy_potato_curriculum/lessons/slide_theme/FRONTEND-STYLE.md) §5 —
the same bar and method as the L01–L07 pass
([2026-07-06-1408-diagram-review-l01-l07.md](2026-07-06-1408-diagram-review-l01-l07.md)): six
parallel per-outline review agents; findings verbatim-condensed.

Checked boxes were applied to the six outlines in the same session (39 `diagram:` directives
added, ~23 reworded for colour intent), plus the deck colour-bug fixes noted below. Still open:
**rebuilding the four L08–L11 decks** from their densified outlines (`build-lecture-deck`; fold the
L1002 classroom-scale transform from the rollout todo into its rebuild), and the L0905 cover badge
question at the bottom.

> **2026-07-06 update — rebuild DONE.** All six decks (L0802 +8 visuals, L0902 +9, L0905 +6 on the
> two-lane wire spine, L1002 +9 incl. the classroom rescale + the 3.3 render gap, L1102 +4,
> L1105 +3 with the 3.1 recolour) rebuilt from the densified outlines and browser-verified:
> per-slide stage-overflow + chrome-collision sweep clean, slide counts match outlines, classroom
> type-scale confirmed. L0905 cover badge resolved as suggested (dashed ink-faint, ⚠ glyph coral).

## Cross-cutting patterns (same as L01–L07, confirmed here)

1. **Colour intent was stated in almost no pre-existing directive** — 1 of 31. The deck builders
   guessed right except in L1002, where they guessed wrong three times (below).
2. **Each lesson owns an obvious motif that stopped after one beat**: L08 the tool-spec card;
   L0902 the client—`←MCP→`—server boundary stack + the break-even chart; L0905 the two-lane
   client↔server wire; L10 the two-node cycle (the course's central picture, debuting here);
   L11 the loop/bracket/ceiling chain (already strong, extended two beats).
3. **Cost claims without charts** recurred: L08 2.1 (per-tool token tax), L1002 5.4 (context
   growth — the *named* staircase example), L0902's verdicts now plot onto its own break-even chart.

## Deck colour bugs (L0505 class — coral on the happy path) — FIXED in this pass

- [x] **L1002 deck cover + slide 2.1** — the back-edge (the lesson's whole claim, and the motif
  every L11 deck re-draws in cyan) rendered **coral**: tools-node strokes, back-edge path/dot/
  arrowhead/label, and the caption's "back-edge" word. All → cyan. Highest-priority fix in range.
- [x] **L1002 deck slide 4.1** — the "graph (L10)" layer box (the failure *handler*, the lesson's
  own contribution) was coral-bordered with a coral wash → cyan.
- [x] **L0802 deck slide 6.2** — `warn-cell` coloured the *correct* model behavior ("stop
  retrying") coral; moved to the failure-example cell.
- Everything else in the four decks checked clean (L0902, L0905, L1102, L1105 all clean;
  L1105 3.1's coral boxes were a faithful render of a flawed directive — fixed at directive level).

## L08 — `L0802_lecture.md` (was 7/26 → now 15/26; spec-card motif never recurred)

- [x] 6.2 error classes — 5.1's fan zoomed into the error branch (cyan fix-loop / cyan dashed
  retry / coral struck-through retry on the stop chip).
- [x] 3.2 description is the field that matters — three spec cards, only the description row lit
  cyan → three outcome chips (cyan called / ink-faint never called / coral misused). Motif debut.
- [x] 2.1 per-tool token tax — bar chart, 5-tool cyan vs 20-tool coral + "wrong-tool pick" tag.
- [x] 9.1 bridge — the 1.2 spec card unchanged inside a dashed MCP envelope ("packaging lands in
  L09"). Motif bookend.
- [x] 5.2 no-data ≠ error — two-up of 5.1's fan: coral merged-shapes + spinning retry vs cyan
  distinct shapes.
- [x] 4.1 tight vs loose schema — contrast two-up, schema row lit.
- [x] 4.3 shape ≠ truth — two-gate flow (cyan schema gate → coral truth gate → "§6" pointer).
- [x] 3.1 names read in isolation — registry pill strip (lower priority).
- [x] Directive fixes: 1.2 (declared the spec-card motif), 3.3, 4.4 (dial → three-position
  spectrum), 5.1 (`{"found": false}` ink-faint *on purpose*), 6.3.

## L09 — `L0902_lecture.md` (was 6/24 → now 15/24; boundary stack + break-even chart each used once)

- [x] 5.3 three scenarios — three labelled dots plotted on 5.2's break-even chart.
- [x] 3.4 failure modes on the wire — 1.4's stack with four coral failure pins (in-server
  credentials pin gets the cyan "well-shaped result, not transport error" callout).
- [x] 3.3 model can't tell it's MCP — flow strip, steps 2–4 in a dashed "invisible" bracket,
  endpoints cyan "same as L07".
- [x] 4.2 design identical / packaging changed — cyan design block spanning both panels; neither
  side coral.
- [x] 1.3 discover/invoke/package — 1.1's fan with the coral glue collapsed to one cyan wire.
- [x] 5.4 gradient, not gate — third break-even beat: a dot graduating rightward; dashed arrow for
  the consumer that never arrives.
- [x] 3.1 two transports — nested-child-process vs across-the-wire two-up, both cyan.
- [x] 2.3 published list = external API — curated 3-chip list cyan vs coral 20-chip flood (4.4
  re-shows it builder-side).
- [x] 6.3 bridge to L10 — loop with dashed-cyan back-edge, inline/MCP chips interchangeable.
- [x] Directive fixes: 1.1, 1.4 (declared the boundary motif), 2.2 (renamed key cyan, not coral),
  3.2, 4.3, 5.2 (declared the section-5 motif).

## L09 — `L0905_lecture.md` (was 3/9 → now 9/9; the two-lane wire is now the spine)

- [x] 2.1 handshake — two-lane client↔server sequence; **motif debut** (4.1 already used the frame).
- [x] 3.2 invisibility — 3.1's round-trip with the MCP hop dimmed, endpoints cyan (explicitly no coral).
- [x] 4.2 recovery is an operator action — 4.1's timeline third beat: stopped box flips back cyan,
  coral only on the historical failed call.
- [x] 2.2 discovery payload zoomed — cyan spec card off the server lane ("the L08 design surface,
  serialized").
- [x] 5.1 recap bookend — full wire small, server lane dashed cyan "you build this side — L0906".
- [x] 1.2 stdio config block (lowest priority; slide had room).
- [x] Directive fixes: 1.1 (inline ink-faint, not coral), 3.1 (cyan-faint invisibility container +
  L07 message-strip echo), 4.1 (dashed-coral stopped server; same frame as 2.1).
- [x] **Open question:** the deck cover's "⚠ shown, not run" badge is **dashed coral** — an
  environment caveat, not a failure; dashed+coral mixes the vocabulary. Suggest dashed ink-faint
  with only the ⚠ glyph coral, at rebuild time. *(Resolved as suggested in the 2026-07-06 rebuild:
  dashed ink-faint box, coral confined to the ⚠ glyph.)*

## L10 — `L1002_lecture.md` (was 4/23 → now 13/23; weakest in range, and the deck painted the cycle coral)

- [x] 1.1 graph-with-a-cycle — two-up vs the L04 chain; the back-edge the only cyan element.
  Motif debut; must match L04/L0505 renderings.
- [x] 3.2 four termination conditions — the 2.1 graph with route's exit fanned (END cyan /
  recursion_limit coral / two dashed ink-faint).
- [x] 3.5 extra caps are just branches — same fan, two dashed branches promoted to cyan.
- [x] 4.3 `handle_tool_errors` — crash (coral raise escaping) vs recover (error pill riding the
  back-edge in cyan) two-up.
- [x] 5.1 graph vs `while` loop — side-by-side with cyan tie-lines; explicitly no coral.
- [x] 5.4 context growth — the cumulative-token staircase (third beat of the L01 8.3 / L07 4.2
  cross-lesson motif).
- [x] 5.5 bridge — the 2.1 graph wrapped in a dashed `create_agent(...)` box with three chips.
  The picture L11 opens on.
- [x] 2.2 `add_messages` — overwrite-vs-append pill-stack two-up (L02/L07 motif).
- [x] 2.5 watch the cycle turn — chunk pill strip with cyan back-edge arcs (lower priority).
- [x] Directive fixes: 2.1 (back-edge **cyan, never coral**; declared the lesson motif), 2.4,
  3.3 (also: this directive was **never rendered in the deck** — a render gap to fix at rebuild),
  4.1 (handler layers cyan; coral only on the raise).

## L11 — `L1102_lecture.md` (was 6/10 → now 10/10) + `L1105_lecture.md` (was 5/9 → now 8/9)

- [x] L1102 3.2 mapping payoff — the 3.1 graph re-drawn with cyan argument chips pinned per piece
  (completes the 3.1 bare → 3.2 annotated → 3.3 traced triple; table stays).
- [x] L1102 4.1 not magic — outer cyan `create_agent(...)` box containing the 3.1 graph; dashed
  "StateGraph → L15" ghost.
- [x] L1102 1.2 shallow vs deep — cyan box vs dashed L18 ghost box (deep ≠ failure).
- [x] L1102 4.3 bridge — small loop labeled "ReAct" + dashed sibling-pattern row (→ L15).
- [x] L1105 2.2 the cap moved inside — 2.1's bracket with the cap chip now *inside* and a coral
  runaway arc. Second bracket beat.
- [x] L1105 3.3 over/under-engineering — 3.2's ceiling with both coral mis-placements. Third
  ceiling beat.
- [x] L1105 3.5 gotcha triptych — the three motifs in miniature (polish).
- [x] Directive fixes: L1102 3.1/2.1/1.1 (colour encoded; motif declared); L1105 1.2 (no coral —
  both runs correct), 3.2, and **3.1 recoloured** — coral moved to the arrows only, destination
  boxes dashed ink-faint "→ L15" (the L0505-bug shape at directive level; deck follows at rebuild).

## Order of attack for the rebuild pass

1. **L1002** — most new directives (9), the colour fixes already in its deck, plus the known
   classroom-scale rescale (one rebuild covers all three; hand-scale the surviving hand-crafted
   diagrams, per the rollout todo).
2. **L0905** (6 adds; the wire motif restructures its visuals), then **L0902** (9 adds) and
   **L0802** (8 adds).
3. **L1102 / L1105** last — newest decks, smallest deltas (4 and 3 adds on already-themed decks).
